<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <sql id="selectColums">
        select id,userName,`passWord`,nick_name,user_sex from users
    </sql>
    <resultMap id="userResultMap" type="com.example.demo.entity.UserEntity">
        <result column="id" property="id"></result>
        <result column="userName" property="userName"></result>
        <result column="passWord" property="passWord"></result>
        <result column="user_sex" property="userSex"></result>
        <result column="nick_name" property="nickName"></result>
    </resultMap>

    <select id="getAll" resultMap="userResultMap">
        <include refid="selectColums"/>
    </select>

    <select id="getOne" parameterType="int" resultMap="userResultMap">
        <include refid="selectColums"/>
        where id=#{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.example.demo.entity.UserEntity">
        insert into users(userName,passWord,user_sex) VALUES(#{userName}, #{passWord}, #{userSex})
    </insert>

    <delete id="delete" parameterType="int">
        DELETE FROM users WHERE id =#{id}
    </delete>

    <update id="update" parameterType="com.example.demo.entity.UserEntity">
        UPDATE users SET userName=#{userName},nick_name=#{nickName} WHERE id =#{id}
    </update>

</mapper>